This is my collection of scripts used for a new Windows install. It includes one-time setup automation scripts, as well as [general scripts to be kept on hand](scripts-to-keep/README.md).

After cloning this repo, run `git update-index --assume-unchanged logs/log.txt` to make sure entries to the log file do not result in git changes.